{
  "name": "examples",
  "version": "4.2.1",
  "private": true,
  "scripts": {
    "demo:basic": "tsnd -r tsconfig-paths/register -r dotenv/config express-basic",
    "demo:express": "tsnd -r tsconfig-paths/register -r dotenv/config express",
    "demo:gcs": "tsnd -r tsconfig-paths/register -r dotenv/config express-gcs",
    "demo:gcs:direct": "tsnd -r tsconfig-paths/register -r dotenv/config gcs-direct",
    "demo:node": "tsnd -r tsconfig-paths/register -r dotenv/config node-http-server",
    "demo:s3": "tsnd -r tsconfig-paths/register -r dotenv/config express-s3",
    "demo:tus": "tsnd -r tsconfig-paths/register -r dotenv/config express-tus",
    "demo:fastify": "tsnd -r tsconfig-paths/register -r dotenv/config fastify"
  },
  "dependencies": {
    "@uploadx/core": "^4.2.1",
    "dotenv": "8.2.0",
    "express": "4.17.1",
    "fastify": "2.15.3",
    "node-uploadx": "^4.2.1",
    "ts-node-dev": "1.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.8"
  }
}
