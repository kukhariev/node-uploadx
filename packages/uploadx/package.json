{
  "name": "node-uploadx",
  "version": "4.2.0",
  "files": [
    "lib/"
  ],
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -b .",
    "clean": "rimraf ./tsconfig.tsbuildinfo && rimraf ./lib"
  },
  "dependencies": {
    "@uploadx/core": "^4.2.0",
    "@uploadx/gcs": "^4.2.0",
    "@uploadx/s3": "^4.2.0"
  }
}
