{
  // extend your base config so you don't have to redefine your compilerOptions
  "extends": "./tsconfig.json",
  "include": ["src/**/*.ts", "test/**/*.ts", "examples/**/*.*"],
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true
  }
}
